apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
#  namespace: loreal
spec:
  type: {{ .Values.service.type }}
  selector:
    #lables
      project: roboshop
      tier: frontend
     # purpose: replicaset
  ports:
  - protocol: TCP
    port: 80 # service port
    targetPort: 80 # container port
